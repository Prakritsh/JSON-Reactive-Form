import { Routes } from '@angular/router';
import { JsonFormContainerComponent } from './features/json-form-container/json-form-container.component';

export const routes: Routes = [
    {
        path : '',
        component: JsonFormContainerComponent
    }
];
